name: Post TopRS (RS14) to Facebook

on:
  schedule:
    # 06:00 (Asia/Ho_Chi_Minh, UTC+7) Mon-Fri = 23:00 UTC Sun-Thu
    - cron: '0 23 * * 0-4'
  workflow_dispatch: {}

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - run: npm ci

      - run: npx playwright install --with-deps chromium

      - run: node src/runTopRS.js
        env:
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
